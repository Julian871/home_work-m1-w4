export type connectType = {
    IP: string,
    URL: string,
    date: number,
    title: string,
    deviceId: string
}

export type connectTypeOutput = {
    ip: string,
    title: string,
    lastActiveDate: string,
    deviceId: string
}